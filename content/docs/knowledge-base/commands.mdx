---
title: Commands
description: Run administrative commands for a self-hosted Coolify instance and find the complete root account recovery guide.
---

# Commands

## Recover the root account

Use the complete recovery guide for the root account command. The guide includes prerequisites, consequences, verification, and failure recovery:

- [Reset Root Password](/core/backup-and-recovery/reset-root-password)

## Delete a stuck service

Connect to the server that runs Coolify over SSH, then run:

```sh
docker exec -it coolify php artisan services:delete
```
